# ECOSYSTEM — 2026-08

Карта: **44 узлов**, **57 рёбер**, **7 замкнутых контуров**. Сгенерировано 2026-08-18.

## Состояние
- Сплошных линий (≥2 источника): **1**, пунктиром: 56.
- Погасших рёбер (источник мёртв): **0**.
- Подтверждено недельным движком: **2**.
- Блёкнущих узлов (>6 мес. без подтверждения): **0**.

## Добавлено
— ничего.

## Отвалилось
— ничего.

## Изменилось
— ничего.

## Замкнутые контуры
- `c1` **sales** — coreweave → nvidia → openai → coreweave
- `c2` **financing** — nvidia → openai → stargate → nvidia
- `c3` **financing** — microsoft → openai → stargate → microsoft
- `c4` **financing** — nvidia → openai → oracle → stargate → nvidia
- `c5` **financing** — microsoft → openai → oracle → stargate → microsoft
- `c6` **sales** — coreweave → nvidia → openai → stargate → microsoft → coreweave
- `c7` **sales** — microsoft → nebius → nvidia → openai → stargate → microsoft

## Слои под одним владельцем
- **L7 ПЕРЕДАЧА ТОКА** — HHI 0.5312, держат: american-electric-power, vertiv
